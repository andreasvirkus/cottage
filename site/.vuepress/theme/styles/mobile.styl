@require './config'

@media (max-width: 35rem)
  .content:not(.custom)
    padding 2rem
  h1
    font-size 1.9rem
  .content:not(.custom)
    padding 1.5rem
  .content
    pre, pre[class*="language-"]
      margin 0.85rem -1.5rem
      border-radius 0
