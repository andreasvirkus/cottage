{{> doc-head }}
<body>
    {{> header }}

    {{> nav }}

    <main>

        <h1>{{ title }}</h1>
        <p>{{ description }}</p>

        {{{ contents }}}
    </main>

    {{> footer }}
</body>
</html>
